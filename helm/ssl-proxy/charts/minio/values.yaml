# Values local to the minio subchart.
global:
  nameOverride: ""
  fullnameOverride: ""
  image:
    registry: ""
    pullPolicy: IfNotPresent
  rolloutRevision: ""
  labels: {}
  annotations: {}
  serviceAccount:
    create: true
    name: "ssl-proxy"
    annotations: {}
  podSecurityContext: {}
  podAnnotations: {}
  nodeSelector: {}
  tolerations: []
  affinity: []

external: true
endpoint: "http://minio.minio.svc.cluster.local:9000"
bucket: "integration-console-exports"
accessKeySecret:
  name: "minio-credentials"
  key: "access-key"
secretKeySecret:
  name: "minio-credentials"
  key: "secret-key"
image:
  repository: minio/minio
  tag: latest
  pullPolicy: IfNotPresent
clientImage:
  repository: minio/mc
  tag: latest
  pullPolicy: IfNotPresent
extraBuckets:
  - ssl-proxy-wireless-raw-archive
initJob:
  activeDeadlineSeconds: 600
  backoffLimit: 3
  resources:
    requests:
      cpu: 25m
      memory: 64Mi
    limits:
      cpu: 250m
      memory: 256Mi
persistence:
  enabled: true
  hostPath: ""
  size: 50Gi
  storageClassName: ""
resources:
  limits:
    cpu: "1"
    memory: 1Gi
  requests:
    cpu: 250m
    memory: 512Mi

# Ollama connection settings (for vec-worker embedding provider)
