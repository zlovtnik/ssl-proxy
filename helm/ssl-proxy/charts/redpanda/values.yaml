# Values local to the redpanda subchart.
global:
  nameOverride: ""
  fullnameOverride: ""
  image:
    registry: ""
    pullPolicy: IfNotPresent
  rolloutRevision: ""
  labels: {}
  annotations: {}
  serviceAccount:
    create: true
    name: "ssl-proxy"
    annotations: {}
  podSecurityContext: {}
  podAnnotations: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}

# -- Set to true if Redpanda is managed externally (not deployed by this chart)
external: true
# -- Bootstrap servers (if external, provide comma-separated list)
bootstrapServers: "redpanda-0.redpanda.redpanda.svc.cluster.local:9092"
# -- Admin API port for metrics and rpk operations
adminPort: 9644
image:
  repository: redpandadata/redpanda
  tag: latest
  pullPolicy: IfNotPresent
# The bundled StatefulSet intentionally supports one broker. Use an external
# Redpanda cluster for multi-broker deployments.
replicas: 1
topicReplicationFactor: 1
initJob:
  activeDeadlineSeconds: 600
  backoffLimit: 3
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi
persistence:
  enabled: true
  hostPath: ""
  size: 20Gi
  storageClassName: ""
resources:
  limits:
    cpu: "2"
    memory: 2Gi
  requests:
    cpu: 500m
    memory: 1Gi

