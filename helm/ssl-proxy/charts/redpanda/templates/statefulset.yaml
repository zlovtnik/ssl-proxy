{{- if not .Values.external }}
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "ssl-proxy.redpanda.fullname" . }}-redpanda
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ssl-proxy.redpanda.labels" . | nindent 4 }}
    app.kubernetes.io/component: redpanda
spec:
  serviceName: {{ include "ssl-proxy.redpanda.fullname" . }}-redpanda
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      {{- include "ssl-proxy.redpanda.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: redpanda
  template:
    metadata:
      labels:
        {{- include "ssl-proxy.redpanda.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: redpanda
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 60
      containers:
        - name: redpanda
          image: {{ include "ssl-proxy.redpanda.image" (dict "root" . "image" .Values.image) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - redpanda
            - start
            - --overprovisioned
            - --smp
            - "1"
            - --memory
            - 1G
            - --reserve-memory
            - 0M
            - --node-id
            - "0"
            - --check=false
            - --kafka-addr
            - INTERNAL://0.0.0.0:9092
            - --advertise-kafka-addr
            - INTERNAL://{{ include "ssl-proxy.redpanda.fullname" . }}-redpanda:9092
          ports:
            - name: kafka
              containerPort: 9092
            - name: admin
              containerPort: {{ .Values.adminPort }}
          readinessProbe:
            exec:
              command: ["rpk", "cluster", "info", "--brokers", "127.0.0.1:9092"]
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /v1/status/ready
              port: admin
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: redpanda-data
              mountPath: /data
      volumes:
        - name: redpanda-data
          {{- if .Values.persistence.hostPath }}
          hostPath:
            path: {{ .Values.persistence.hostPath | quote }}
            type: Directory
          {{- else if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "ssl-proxy.redpanda.fullname" . }}-redpanda-data
          {{- else }}
          emptyDir: {}
          {{- end }}
{{- end }}
