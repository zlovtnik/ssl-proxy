global:
  rolloutRevision: ""
  serviceAccount: {name: ssl-proxy}
enabled: true
image: {repository: redis, tag: 7.4.3-alpine, pullPolicy: IfNotPresent}
credentialsSecret: {name: redis-runtime, passwordKey: password}
resources:
  requests: {cpu: 50m, memory: 64Mi}
  limits: {cpu: 500m, memory: 256Mi}
