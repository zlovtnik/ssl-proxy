# Schema Archive

These files were executable schema fragments before the May 2026 two-file schema rewrite.
They are retained for reference only. Active schema baselines now live in `sql/postgres.sql` and `sql/oracle.sql`.

Legacy files under this directory are not active migrations. Do not run them for new environments; use only the canonical baselines.
