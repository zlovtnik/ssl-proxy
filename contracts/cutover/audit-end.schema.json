{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ssl-proxy.invalid/contracts/cutover/audit-end.schema.json",
  "title": "Fresh TiDB bounded audit end",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "kind", "cluster_id", "captured_at", "cutover_sha256", "partitions"],
  "properties": {
    "schema_version": {"const": 1},
    "kind": {"const": "audit_end"},
    "cluster_id": {"type": "string", "minLength": 1},
    "captured_at": {"type": "string", "format": "date-time", "pattern": "Z$"},
    "cutover_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "partitions": {
      "type": "array",
      "minItems": 1,
      "items": {"$ref": "#/$defs/partition"}
    }
  },
  "$defs": {
    "partition": {
      "type": "object",
      "additionalProperties": false,
      "required": ["group_id", "topic", "partition", "end_offset"],
      "properties": {
        "group_id": {"type": "string", "minLength": 1},
        "topic": {"type": "string", "minLength": 1},
        "partition": {"type": "integer", "minimum": 0},
        "end_offset": {"type": "integer", "minimum": 0}
      }
    }
  }
}
